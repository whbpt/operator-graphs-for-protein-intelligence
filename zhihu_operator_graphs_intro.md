# 从 Potts 到 AlphaFold：蛋白质智能中的图、能量与算子

我最近一直在想一个很具体的问题：当不同方法都在两个残基之间画出一条边时，它们究竟共享了什么？

这个问题看起来像是在抠字眼。蛋白质领域早就习惯了画图：结构里有接触图，MSA 里有协同进化图，Potts 模型里有耦合，GCN 里有邻接矩阵，Transformer 里有 attention，AlphaFold 里有 pair representation。最后大家画出来的，往往都是一个 $L\times L$ 的热图。颜色深的地方被叫作“强相互作用”，再和 contact map 叠一下，故事似乎就讲通了。

但我越看这些工作，越觉得真正的问题正藏在这种相似里。同样是 $(i,j)$，结构接触说的是两个位置在某个三维构象下足够近；Potts 耦合说的是一个家族分布中的成对参数；逆协方差试图扣掉其他位置后再描述条件关系；attention 给出一次计算中的路由权重；把 $j$ 位点换成另一种氨基酸，再观察模型对 $i$ 的输出变化，得到的则是一个干预定义的响应。

它们共享端点，却未必共享边的含义。

这也是我写《Operator Graphs for Protein Intelligence》这本书的起点。我想给这些看起来相似的对象建立一套共同坐标，然后逐项说明：哪里真的相通，哪里只是公式长得像，哪一步开始需要额外假设。每一次跨领域翻译都应当带着自己的条件；“统一”不负责把所有模型改叫统计物理。

## 一条边，至少有五种对象

先考虑蛋白质序列中的两个位置 $i$ 和 $j$。

在结构图中，边可以是一个二值接触，也可以携带距离、方向和局部坐标系之间的关系。它回答的是几何问题。

在 Potts 模型中，边携带的是一个 $q\times q$ 的表 $J_{ij}(a,b)$。这里 $q$ 是残基字母表大小。这个表描述：在拟合出来的家族联合分布中，残基组合 $(a,b)$ 如何进入统计能量。

在逆协方差方法中，边来自精度矩阵 $\Omega=\Sigma^{-1}$ 的一个块。高斯模型里，$\Omega_{ij}=0$ 有严格的条件独立含义；到了类别型序列中，one-hot 编码、规范自由度、正则化和近似方式都会进入解释。

在 attention 中，边通常是

$$
\alpha_{ij}
=\operatorname{softmax}_{j}\!\left(\frac{q_i^\top k_j}{\sqrt d}\right).
$$

它回答的是：对固定的目标位置 $i$，这一层、这个头把多少归一化权重分配给来源 $j$。这是计算路径，不自动等于输出响应，更不自动等于物理接触。

还可以直接审计一个训练好的模型。固定其余序列背景 $C$，改变 $j$ 位点的残基，观察 $i$ 位点各类别的预测如何变化，得到

$$
G_{i\leftarrow j}(C;a,b).
$$

这条边是有方向的，携带着一张“源残基对目标残基分布的作用表”。如果换一个序列背景 $C$，整张表还可能跟着变化。

实验里的 double-mutant epistasis 又是另一件事：它依赖具体干预、具体表型以及选定的加性零假设。它提供生物学证据，却不等同于前面任何一个模型内部量。

所以，判断两篇文章是不是在做同一件事，不能只看它们是否都输出了一个矩阵。至少要继续问七个问题：状态空间是什么，边上携带什么对象，参考测度是什么，softmax 沿哪条轴归一化，系统怎样更新，参数怎样估计，最后用什么外部终点验证。

很多所谓的方法代际差异，拆到这里以后并没有想象中大；很多看起来只差一个公式的工作，拆到这里以后反而不是同一个问题。

## 标量边只是算子的一道影子

蛋白质残基是类别变量。假设 $j$ 位点从亮氨酸换成谷氨酸，对 $i$ 位点的影响不会天然只有“强”或“弱”；它可能提高某些残基的概率，同时压低另一些残基。最自然的边对象因此是一张类别对类别的响应表，也就是从源位置对比空间到目标位置对比空间的线性算子：

$$
\mathcal K_{i\leftarrow j}:\mathcal H_j^0\longrightarrow\mathcal H_i^0.
$$

这里的 $\mathcal H_i^0$ 可以直观地理解为“位置 $i$ 上去掉整体常数后的残基差异空间”。同一个算子可以被压缩成 Frobenius 范数、最大奇异值、top-$k$ 边或者一张 contact score 图，但压缩会丢掉方向：两条边可以具有相同范数，却偏好完全不同的氨基酸替换；也可以有相同接触分数，却对某个具体突变给出相反预测。

这一步还有一个容易被忽略的问题。类别型成对表并不天然唯一。常数项、只依赖 $a$ 的项和只依赖 $b$ 的项，可以在单点场和双点耦合之间来回搬运，而不改变最终概率。这就是 Potts 模型里熟悉的 gauge freedom。

要比较不同模型的边，必须先声明参考分布 $p_i,p_j$，再把单点方向投影掉。写成矩阵，就是

$$
G_{i\leftarrow j}=P_iT_{i\leftarrow j}P_j^\top,
\qquad
P_i=I-\mathbf 1p_i^\top.
$$

这样得到的 $G$ 才是相对于该参考测度的纯双变量对比。参考测度不是装饰：用均匀背景、Pfam 家族频率、模型边缘分布或者条件背景，得到的是不同几何下的同一类问题。

这就是书里的中心对象：参考测度中心化的算子值图。位置携带类别对比空间，方向边携带空间之间的映射，普通的标量图只是它的某种压缩。

## 为什么 Potts、逆协方差总像是在反复出现

协方差本身会把直接和间接关联混在一起。如果 $i$ 通过 $k$ 影响 $j$，即使 $i$ 和 $j$ 之间没有直接项，也可能看到很强的相关。逆协方差最有吸引力的地方，就是在高斯模型中给出了一个干净答案：精度矩阵的稀疏结构对应条件独立图。

早期 DCA 的很多数学直觉都来自这里。均场近似下，Potts 耦合与协方差逆矩阵之间出现近似关系；伪似然方法则改为逐位置拟合条件分布。算法形式变了，核心愿望很稳定：从全局共变中扣除可由其他位置解释的部分，恢复更接近直接条件作用的量。

所以我理解为什么很多论文在研究者眼里“区别都不大”。换一种编码，做一次正则化，换一个低秩分解，再接一个分类器，最后仍然可能是在同一份二阶统计量上做重参数化。它们当然可以带来工程收益，尤其是在小样本、噪声和计算复杂度上；但如果估计对象、验证终点和可识别假设都没有变化，方法的理论地位就没有发生根本改变。

同时也要守住边界。高斯精度矩阵、类别型 Potts 耦合和神经网络干预响应不是普遍等价的。它们只有在明确的编码、近似、参考规范和模型族下才可能建立对应。熟悉的矩阵求逆是一台很有生命力的发动机，但不能靠图像相似直接升级成生物机制。

## GCN 的几代公式，真正增加了什么

GCN 的演化特别适合观察“一个公式到底在什么空间里工作”。最早的 spectral graph convolution 从图 Laplacian

$$
L=U\Lambda U^\top
$$

出发，把节点信号 $x$ 变换到图 Fourier 基底，在谱上施加滤波器，再变换回来：

$$
g_\theta(L)x=Ug_\theta(\Lambda)U^\top x.
$$

Chebyshev 多项式把全局谱滤波近似成局部传播。Kipf--Welling GCN 再做一阶截断和重归一化，得到今天最熟悉的形式：

$$
H^{(\ell+1)}
=\sigma\!\left(
\widetilde D^{-1/2}\widetilde A\widetilde D^{-1/2}
H^{(\ell)}W^{(\ell)}
\right).
$$

这里的图是预先给定的，邻居权重主要由拓扑和度决定。层数加深，相当于反复平滑图上的节点信号，oversmoothing 正是这套扩散偏置的直接后果。

GraphSAGE 把固定矩阵乘法改写成对邻居集合的可学习聚合，解决采样和归纳泛化问题。GAT 又让路由依赖当前节点状态：

$$
\alpha_{ij}
=\operatorname{softmax}_{j\in\mathcal N(i)}e(h_i,h_j).
$$

再往后的 message passing neural network 把消息写成

$$
m_{ij}=\phi(h_i,h_j,e_{ij}),
\qquad
h_i'=\psi\!\left(h_i,\sum_jm_{ij}\right),
$$

边属性、坐标、方向、距离和更高阶几何都可以进入消息函数。到了分子图网络和等变网络，节点与边不再只是无类型标量，空间变换规律也被写进架构。

这条演化线真正增加的是坐标：固定谱滤波、局部聚合、状态依赖路由、带类型的消息、几何等变性。它并没有自动增加物理意义。一个 GAT 权重首先仍是邻居集合上的归一化计算权重；一个等变更新首先保证旋转和平移时输出按规定变化。要把它们解释为概率耦合、能量、力或者生物机制，还需要相应的联合分布、动力学和实验验证。

小波也可以放在这条线上理解。经典图小波通过 $g(sL)$ 在不同尺度选择谱段，它处理的是相对于某个 Laplacian 的图频率。低频表示沿强边变化缓慢，高频表示局部变化快。这个“频率”由图算子定义，不是蛋白质天然携带的一组生物学振动模式。只有当图、测度和动力学生成元与真实体系建立了关系，谱模态才获得更强的物理解释。

## AlphaFold 三代，变化的是边在系统中的地位

沿着同一套坐标看 AlphaFold，会看到一条比“模型越来越大”更有意思的演化线。

AlphaFold 1 先把 MSA 压缩成 profile、协同进化和其他手工构造的一维、二维特征，再预测每对残基的距离分布。它的边对象很明确：一条边携带局部几何分布。随后通过背景校正把 distogram 转成有效势，用优化方法实现三维坐标。这个势是合理的统计优化目标；把它进一步读成热力学自由能或可迁移的分子力场，需要额外校准。

AlphaFold 2 把 MSA 行保留在网络内部，并让 MSA state 与 pair state 在 Evoformer 中反复交换信息。$z_{ij}$ 不再只是最终输出前的一张特征图，而是具有生命周期的边记忆。outer product mean 把行级信息写入 pair state，triangle multiplication 和 triangle attention 让 $(i,j)$ 通过路径 $(i,k,j)$ 更新。这里确实发生了三索引计算，但输出仍可以存回一条 pair edge；它是否对应不可约三体能量，要看对输出做低阶剔除后的干预结果，而不能由 triangle 这个名字决定。

AlphaFold 3 进一步扩展了节点类型：蛋白质、核酸、配体、离子和修饰都进入同一个异质分子系统。MSA 在较短的模块中把信息沉积到 pair state，随后显式行轴被丢弃；pair representation 成为主要的关系记忆，并条件化一个全原子扩散生成器。

扩散模型的数学核心可以压成三步：

$$
\text{多尺度密度平滑}
\longrightarrow
\text{条件 score 估计}
\longrightarrow
\text{反向概率输运}.
$$

正向 SDE

$$
\mathrm dR_t=f(R_t,t)\,\mathrm dt+g(t)\,\mathrm dW_t
$$

按预设噪声日程破坏坐标信息；网络学习噪声尺度 $t$ 下的

$$
s_t(R)=\nabla_R\log p_t(R\mid s,z),
$$

再通过反向 SDE，或者 probability-flow ODE，把简单噪声分布输运回结构分布。在 score 精确的条件下，两种动力学具有相同的单时刻边缘分布。SDE 的路径持续注入布朗随机性；ODE 在给定初值后沿确定路径演化。二者共享边缘概率流，却不共享路径分布。

这里尤其容易出现语言越界。diffusion 的时间首先是人工噪声尺度，score 首先是训练分布的对数密度梯度，采样轨迹首先是算法生成路径。要把 score 叫作物理力，把反向轨迹叫作分子动力学，需要温度、测度、平衡分布、迁移率和真实时间标定等更强条件。

从 AF1 到 AF3，可以概括成：显式的 pair geometry，变成持续演化的 pair state，再变成条件化生成坐标的异质 pair state。边越来越重要，也越来越不自带解释。模型把更多证据整合进同一表示，预测能力由此提高，证据的来源却更难从一个通道或一张热图里读出来。

我并不认为 AlphaFold 是从这套算子框架“推导”出来的。书中对三代 AlphaFold 的分析是一种回顾性的结构审计：用相同坐标检查它们保留什么状态、在哪里压缩信息、边携带什么、如何组合路径、在哪个空间归一化、输出允许什么解释。它可以揭示架构判断的连续性，不能替代关于设计者思想来源的历史证据，更不是三代算法之间的等价定理。

## 神经网络里的“动态边”究竟多了什么

Potts 模型给出一张固定的家族耦合表。非线性蛋白质语言模型更自然地产生背景依赖响应：

$$
G_{i\leftarrow j}(C)
=\overline G_{i\leftarrow j}
+\delta G_{i\leftarrow j}(C).
$$

$\overline G$ 是在所声明背景分布下，描述该响应的最佳静态算子；$\delta G(C)$ 是具体序列背景造成的偏离。于是可以分别问：模型是否保留了稳定的家族式二阶关系？它有多少行为必须依赖周围序列才能描述？

这个分解有一个很普通、也很可靠的勾股恒等式：

$$
\mathbb E_C\|G(C)\|^2
=\|\overline G\|^2
+\mathbb E_C\|\delta G(C)\|^2.
$$

交叉项消失，只因为残差在同一个背景分布下均值为零。因此，这个静态—动态分解对任意声明的背景律都成立，包括有相关性的真实 MSA 分布。

更强的一步，是把动态方差解释为所有包含 $i,j$ 和其他位置的高阶 functional-ANOVA 质量之和。这一步需要独立乘积参考。真实 Pfam MSA 中存在系统发育、亚家族和功能约束导致的相关；在这样的经验分布下，动态方差仍然可测，但不同阶成分不再彼此正交，会出现交叉项。此时“模型有多少背景依赖”和“这些依赖分别属于几阶相互作用”是两个不同难度的问题。

我把这条边界写得比较细，是因为统一框架最危险的地方，恰恰是统一得太快。一个模型能产生动态响应，并不等于我们已经识别了高阶生物机制；一个图谱和实验 contact 高度相关，也不等于图上每条边都获得了物理身份。数学分解解决可定义性，估计器解决有限数据下的恢复，实验才决定其中哪些量对应真实世界的干预结果。

## 最后：我们真正需要统一的是什么

回到开头的问题。当 Potts、逆协方差、GCN、attention、AlphaFold 和突变实验都在两个残基之间画边时，它们真正共享的，首先是一组端点和一种“关系应当被显式表示”的建模判断。

再往下，能共享多少，要逐层检查。它们可能共享二阶统计结构，却不共享概率模型；可能共享计算拓扑，却不共享输出响应；可能共享谱，却不共享生成这个谱的动力学；可能都能预测接触，却通过不同证据和不同捷径做到。

我现在更愿意把“统一”理解成建立可比较的坐标。对象之间的等价关系仍要逐项证明。好的统一语言应该让差异变得更精确，也让真正的一致更难被偶然热图蒙混过去。

这本书的第二部分是定理—证明层面的算子几何；第三部分则是混合的，其中一部分给出在明确假设下的模型对应，另一部分用同一语言审计已有架构。它目前仍是持续修订的 v0.9 草稿。我把完整源码和 PDF 放在 GitHub，想先让这套坐标接受公开检验：哪些对应是真的，哪些条件还没有写全，哪些实验能够把“像一个相互作用”推进到“确实是我们声称的那个相互作用”。

- 项目主页：[operator-graphs-for-protein-intelligence](https://github.com/whbpt/operator-graphs-for-protein-intelligence)
- 在线 PDF：[《Operator Graphs for Protein Intelligence》v0.9](https://github.com/whbpt/operator-graphs-for-protein-intelligence/blob/main/output/pdf/operator_graphs_for_protein_intelligence.pdf)
