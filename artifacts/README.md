# Published Artifacts

`published/` mirrors the user-facing deliverables from the workspace-level `outputs/` directory.
The editable sources remain in `paper/` and `docs/`.

The mirror is kept inside the project so the research package remains understandable without
searching the surrounding workspace. It is excluded from Git because the files are generated.
